listen: :${APP_PORT}
trafficStats:
  listen: :${API_PORT}

tls:
  cert: /etc/server.crt
  key: /etc/server.key
auth:
  type: ${AUTH_TYPE}
  password: ${AUTH_PASSWORD}
  http:
    url: ${AUTH_HTTP_URL}?ip=${NODE_IP}
ignoreClientBandwidth: false
masquerade:
  type: proxy
  proxy:
    url: https://www.bing.com
    rewriteHost: true
# outbounds:
#   - name: my_outbound
#     type: socks5
#     socks5:
#       addr: 127.0.0.1:1080 
#       username: admin 
#       password: 123456